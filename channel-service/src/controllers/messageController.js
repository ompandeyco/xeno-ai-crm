// messageController.js
// Responsibility: Receive the "send this message" HTTP request from CRM Backend.
// It reads the channel (whatsapp/email/sms), calls the delivery simulator,
// then triggers the async callback to CRM.

// Will be implemented in the next step.
